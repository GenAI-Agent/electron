# Weaviate 向量資料庫配置範例
# 複製此檔案為 .env 並根據您的需求修改

# ======================
# 基本配置
# ======================

# 部署模式: local, cloud, embedded
WEAVIATE_MODE=local

# 本地部署配置
WEAVIATE_HOST=localhost
WEAVIATE_PORT=8080
WEAVIATE_SCHEME=http

# ======================
# 雲端部署配置 (WCS)
# ======================

# Weaviate Cloud Services 配置
# WEAVIATE_MODE=cloud
# WEAVIATE_CLUSTER_URL=https://your-cluster.weaviate.network
# WEAVIATE_API_KEY=your-weaviate-api-key

# ======================
# 認證配置
# ======================

# 基本認證 (如果需要)
# WEAVIATE_USERNAME=your-username
# WEAVIATE_PASSWORD=your-password

# ======================
# AI 模型配置
# ======================

# OpenAI 配置 (用於 text2vec-openai vectorizer)
# OPENAI_APIKEY=your-openai-api-key

# Cohere 配置 (用於 text2vec-cohere vectorizer)
# COHERE_APIKEY=your-cohere-api-key

# Hugging Face 配置 (用於 text2vec-huggingface vectorizer)
# HUGGINGFACE_APIKEY=your-huggingface-api-key

# ======================
# 進階配置
# ======================

# 連線超時時間 (秒)
WEAVIATE_TIMEOUT=30

# 啟動等待時間 (秒)
WEAVIATE_STARTUP_PERIOD=5

# 本地資料存儲路徑 (用於 embedded 模式)
WEAVIATE_DATA_PATH=./weaviate_data

# Weaviate 版本 (用於 embedded 模式)
WEAVIATE_VERSION=1.22.4

# ======================
# 日誌配置
# ======================

# 日誌級別: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# 日誌檔案路徑
LOG_FILE=vectordb.log

# ======================
# 應用程式配置
# ======================

# 預設批次大小
DEFAULT_BATCH_SIZE=100

# 預設搜尋限制
DEFAULT_SEARCH_LIMIT=10

# 預設向量距離計算方式: cosine, dot, l2-squared, manhattan, hamming
DEFAULT_VECTOR_DISTANCE=cosine
